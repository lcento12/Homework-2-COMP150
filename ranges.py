def rangeRover():
    x = range(1,5)

    for n in x:
        print(n)

rangeRover()

def walkerTexasRanger():
    y = int(input("Enter a number: "))
    z = range(1, y+1)

    for n in z:
        print(n)

    q = random.randrange(z)
    t = random.randrange(z)
    p = random.randrange(z)
    i = random.randrange(z)
    e = random.randrange(z)

    qtpie = [q, t, p, i, e]


walkerTexasRanger()

    
